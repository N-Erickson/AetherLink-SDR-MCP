[pytest]
# Only look for tests in the tests directory
testpaths = tests

# Ignore other directories
norecursedirs = examples docs docker .git venv

# Python files to treat as tests
python_files = test_*.py

# Python classes to treat as tests  
python_classes = Test*

# Python functions to treat as tests
python_functions = test_*